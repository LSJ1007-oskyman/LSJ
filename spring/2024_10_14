* Mapper파일로 데이터를 전달할때는 1개의 값만 전달할 수 있으므로 이때 2개 이상의 값을 전달하기 위해서는 다음과 같은 방법을 사용한다.
1 . 데이터가 파생데이터면 필요한 데이터 1개만 전달하여 mapper파일에서 계산한다.
2 . Map을 이용해서 전달한다.
3 . DTO클래스에 해당 데이터 필드를 추가해서 넣어서 전달한다.

* controller에서 DB연동을 하지 않고 바로 view로 넘어오는 경우에서 넘어가는 값이 있을때 controller에서 굳이 값을 받고, 다시 넘겨주는 과정을 거치지 않고 넘어간 view페이지에서 
  ${param.파라미터명}을 하면 값이 넘어온다.
